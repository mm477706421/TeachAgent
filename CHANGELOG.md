# 更新记录

## main — HTTP 兼容服务支持（2026-09-18）

- OpenAI 兼容服务允许使用 HTTP 域名或 IP 地址，支持保存、连接测试和课堂追问。
- 更新界面与部署文档，继续校验非法协议、URL 内嵌凭据、查询参数和片段。

## main — 响应式布局更新（2026-09-18）

- 宽屏侧栏支持手动收起/展开，768–1199px 默认显示图标栏，手机使用抽屉导航。
- 侧栏独立滚动，矮窗口隐藏辅助展示卡；修复底部设置与退出入口超出屏幕的问题。
- 手机抽屉支持遮罩、关闭按钮、Esc、焦点循环、返回焦点及背景滚动锁定；跨断点自动恢复。
- 补齐所有宽度的搜索入口、表格区域滚动、窄屏表单、图表图例换行、详情标签滚动和弹窗高度约束。
- 新增 7 种视口与导航交互回归，本地版 12 条、Pages 版 9 条浏览器测试通过。

## 1.1.0 — 2026-09-18

- 新增按教师账号独立的 OpenAI 兼容模型配置、草稿连接测试、参数设置与文本发送同意。
- API Key 使用本地 Fernet 加密、接口不回显、支持清除；修改地址不沿用旧密钥。
- 教研追问支持本机 Ollama、本地规则与兼容在线服务，展示模型来源，在线失败明确报错。
- Pages 提供模型设置只读操作预览，不收取密钥、不连接模型。
- 修复短视频常规采样可能没有抽帧的问题，增加首帧补抽。
- 加密依赖固定到 50.0.1，完成后端、真实离线 ASR、浏览器与 Pages 回归，更新功能文档和系统检验报告。

## 1.0.0 — 2026-09-15

首个完整功能版本。

- 新增教师登录、本地会话、密码管理、账号级课堂隔离与学校管理入口。
- 新增课堂视频上传、FFmpeg 抽帧与音轨处理、离线 Whisper 中文转写、顺序任务队列与失败重试。
- 新增 TXT / SRT / VTT 导入、文本清洗、结构化蒸馏、教学环节时间轴与教学画像。
- 新增关联原文证据的优化建议、片段定位、本机 Ollama 追问与明确标识的规则降级。
- 新增 Markdown / JSON / HTML 报告、按账号 ZIP 备份与本地审计。
- 新增墨绿暖白响应式 WebUI、只读合成示例、Docker 与内网反向代理配置。
- 提供后端与浏览器验证、功能部署文档和 GitHub CI。

模型权重与真实课堂资料不包含在 Release 中。深度视觉、声纹分离和分布式处理列入扩展。
